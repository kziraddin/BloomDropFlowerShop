# Base URL of the Django backend.
# Local development (Django running on port 5500):
REACT_APP_API_URL=http://localhost:5500
# Production (Vercel): set this to the Render backend URL, e.g.
# REACT_APP_API_URL=https://bloomdrop-backend.onrender.com
